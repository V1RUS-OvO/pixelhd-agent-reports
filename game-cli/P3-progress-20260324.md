# Phase 3：撤离成功 + 简易支援 API（game-cli）

## Progress 2026-03-24 (code-start-01)

- 已执行 `director-commands-20260324-02` 与总攻调度令 v2026-03-24-ALL，按最高优先级在 `C:/Users/KAKA/pixelhd` 源码开始实现 Destroy → Extraction → mission_success → GameOver 闭环。
- 代码现状梳理：
  - `core/src/mindustry/game/pixelhd/PixelHDMissionLoader.java` 已在 Destroy 0.1 垂直切片基础上，为 PixelHD 模式注入：
    - 以 waveTeam core 为目标的 `DestroyBlockObjective`，完成时写入 `flagExtractionUnlocked`；
    - 以 defaultTeam core 附近为中心的 `ExtractObjective` 子目标（半径 10 tile，依赖 `flagExtractionUnlocked`）。
  - `core/src/mindustry/game/MapObjectives.java` 中：
    - `ExtractObjective.qualified()` 会在解锁时写入 `flagExtractionUnlocked`；
    - `ExtractObjective.done()` 已实现：若 `successOnComplete == true`，则写入 `flagMissionSuccess` 并触发 `GameOverEvent(defaultTeam)`，从而经事件监听将 `MissionRuntime.success=true` 且 phase=mission_success；
    - `resetMissionRuntime` / `setMissionOutcome` 保持原有 flag、phase 推导逻辑不变，以事件为驱动。
  - `core/src/mindustry/game/pixelhd/PixelHDStratagems.java` 已提供两个静态支援入口，在 world 存在时于指定坐标生成不同半径的 `Fx.spawnShockwave` 可见效果。
  - 测试侧已有：
    - `tests/src/test/java/mindustry/game/pixelhd/PixelHDMissionLoaderTest.java`：覆盖 Destroy→extraction_unlocked 与 ExtractObjective 完成时的 mission_success+GameOver 行为；
    - `tests/src/test/java/mindustry/game/pixelhd/PixelHDStratagemsTest.java`：对 `callSupport1/2` 做烟雾测试，保证在初始化 world 后调用不抛异常。
- 本轮首次在源码仓库尝试执行相关测试命令：
  - 在 `C:/Users/KAKA/pixelhd` 下运行：
    - `./gradlew :tests:test --tests "mindustry.game.pixelhd.PixelHDMissionLoaderTest.*"`
    - `./gradlew :tests:test --tests "mindustry.game.pixelhd.PixelHDStratagemsTest.*"`
  - 当前失败原因（编译阶段）：
    - `:tests:compileTestJava` 报找不到若干符号（Events、PixelHDMissionLoader、PixelHDStratagems、Map.copy 等），说明 tests 模块的依赖/导入尚未完全对齐现有源码结构（可能是测试类包声明或类路径与源码位置不一致）。

## Files touched (read/inspected)

- `core/src/mindustry/game/MapObjectives.java`
- `core/src/mindustry/game/pixelhd/PixelHDMissionLoader.java`
- `core/src/mindustry/game/pixelhd/PixelHDStratagems.java`
- `tests/src/test/java/mindustry/game/pixelhd/PixelHDMissionLoaderTest.java`
- `tests/src/test/java/mindustry/game/pixelhd/PixelHDStratagemsTest.java`

> 本次仅为读取与测试命令尝试，尚未对源码做具体修改，下一步将修复 tests 依赖/导入问题，使闭环相关测试可在本地构建中稳定通过。

## Next

- 在 `tests` 模块内对齐 PixelHD 相关测试类与核心源码的包名/依赖：
  - 确认 `PixelHDMissionLoaderTest` / `PixelHDStratagemsTest` 的 package 与 import 与实际类路径一致；
  - 解决编译错误后重新运行上述两条 Gradle 命令，验证 Destroy→Extraction→mission_success→GameOver 闭环的自动化测试通过。
- 若构建继续提示缺失 Arc 等依赖，按需要在 pixelhd 工程外侧补全 Arc 仓库或调整 Gradle 依赖声明，并在后续报告中记录具体步骤。
