# Phase 3：撤离成功 + 简易支援 API（game-cli）

## Progress 2026-03-24 (exec-01)

- 已执行 `director-commands-20260324-03`，确认本轮 game-cli 任务聚焦：
  - 补完 Destroy → Extraction 的失败路径，使超时 / 玩家或 defaultTeam core 全灭时进入 `mission_failed` 并触发 GameOverEvent；
  - 将 `ExtractObjective.done()` 中的 GameOver 行为限定为仅在 PixelHD 模式（或显式标记的任务）下生效，避免影响非 PixelHD 地图；
  - 跑通并保持 `./gradlew :tests:test --tests "mindustry.game.pixelhd.*"` 全绿。
- 代码 & 测试现状梳理（执行前基线）：
  - `core/src/mindustry/game/MapObjectives.java`：
    - `syncMissionRuntime(...)` 已基于 Rules.objectiveFlags 推导 MissionRuntime 的 success/failed/extraction* 状态与 MissionPhase；
    - `ExtractObjective.qualified()` 解锁撤离时写入 `flagExtractionUnlocked`；
    - `ExtractObjective.done()` 当前无条件在 `successOnComplete == true` 时写入 `flagMissionSuccess` 并 fire `GameOverEvent(defaultTeam)`，该行为对所有使用 ExtractObjective 的地图生效，需要按 PixelHD 模式/flag 收紧；
  - `core/src/mindustry/game/pixelhd/PixelHDMissionLoader.java`：
    - 仅在 `pixelhd.mode == "destroy_0_1"` 时注入 DestroyBlockObjective + ExtractObjective 子目标，并初始化 MissionRuntime；
  - `core/src/mindustry/game/pixelhd/PixelHDStratagems.java`：
    - 已提供最小可见支援效果（spawnShockwave），为 content-cli 对接 HUD 按钮提供入口；
  - `tests/src/test/java/ApplicationTests.java`：
    - 提供 PixelHD 测试所需的 headless 环境（加载基础内容、初始化 world/testMap 等）；
  - `tests/src/test/java/mindustry/game/pixelhd/PixelHDMissionLoaderTest.java`：
    - 覆盖 Destroy 完成后 `extraction_unlocked`，以及手动完成 ExtractObjective 时的 mission_success + GameOver；
  - `tests/src/test/java/mindustry/game/pixelhd/PixelHDStratagemsTest.java`：
    - 覆盖 `callSupport1/2` 的“不会抛异常”烟雾测试。
- 构建/测试现状：
  - 在 `C:/Users/KAKA/pixelhd` 执行：
    - `./gradlew :tests:test --tests "mindustry.game.pixelhd.PixelHDMissionLoaderTest.*"`
    - `./gradlew :tests:test --tests "mindustry.game.pixelhd.PixelHDStratagemsTest.*"`
  - 当前失败于 `:tests:compileTestJava`，报若干类/方法找不到（Events、PixelHDMissionLoader、PixelHDStratagems、Map.copy 等），推测为 tests 模块依赖/导入配置尚未完全对齐最新源码（但不影响本轮继续在 core/PixelHD 侧实现逻辑；后续需一并修复 tests 依赖）。

## Plan for execution (game-cli)

- 在 `MapObjectives.ExtractObjective` 中引入“是否触发 GameOver”的显式开关：
  - 通过 `Rules.tags` 或 ExtractObjective 自身字段（例如 `boolean pixelhdGameOver`) 标记 PixelHD 任务；
  - 在 `PixelHDMissionLoader.initPixelHDMission(...)` 注入 ExtractObjective 时设置该开关；
  - 在 `done()` 中仅当该开关为 true 时 fire GameOverEvent(defaultTeam)。
- 设计/实现 Destroy → Extraction 失败条件：
  - 基于 MissionRuntime / Rules.objectiveFlags 约定一种超时或核心全灭的失败源，并写入 `flagMissionFailed`；
  - 在 `syncMissionRuntime(...)` 中已支持 `failed` 分支及 `MissionPhase.mission_failed`，利用这一机制完成失败路径闭环；
- 在 tests 模块中新增/更新用例：
  - 为 `mission_failed` 分支补一条集成测试，模拟 Destroy 完成后触发失败条件并断言 MissionPhase = mission_failed；
  - 修复当前 PixelHD 相关测试类的依赖/导入问题，确保 `mindustry.game.pixelhd.*` 测试集可编译并通过。

> 本次为执行阶段起始梳理与具体实施计划，尚未修改源码；后续每完成一小步实现/测试，将在本文件或新的 P3 报告中追加记录并按协议提交。
