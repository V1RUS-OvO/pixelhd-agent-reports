# P4 Sync - 2026-03-24 (content-cli)

## Identity

- CLI identity: `content-cli`
- Identity source: `content-cli/WHOAMI.md`

## Director Goal Readback

- Current top-level goal: make the Destroy -> Extraction phase readable in HUD, and give mobile Stratagem buttons visible support behavior.
- Current content scope:
  - calibrate `MissionPhase` HUD text for Landing / Objective / Extraction_Unlocked / Extraction_Called / Success / Fail;
  - replace placeholder extraction countdown with real runtime data when available, otherwise keep placeholder and mark it clearly;
  - keep the 3-button mobile Stratagem layout, only show it in PixelHD + mobile, and wire support 1/2/3 to visible in-game effects;
  - update `docs/design/ui-hud-0_1.md` and keep report-side verification notes current.

## This Upload

- Pulled latest instructions from `pixelhd-agent-reports` before work.
- Re-read identity and director command files.
- Uploaded this sync note so the main CLI can confirm `content-cli` is online and tracking the latest order set.

## Notes

- Existing implementation report remains in `content-cli/P3-hud-and-stratagems.md`.
- Current upload only covers identity/director-command acknowledgement, without new code changes.
